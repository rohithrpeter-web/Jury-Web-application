[build]
builder = "NIXPACKS"

[build.nixpacksPlan.phases.setup]
nixPkgs = ["nodejs_22", "python3", "pkg-config", "gcc"]

[build.nixpacksPlan.phases.install]
cmds = ["npm ci --ignore-scripts", "npm rebuild better-sqlite3 --build-from-source"]